NODE_ENV=development
PORT=3000
DB_HOST=localhost
DB_PORT=5432
DB_NAME=timeline_app
DB_USER=timeline_user
DB_PASSWORD=timeline_password
# Frontend-Origin für CORS (ohne Schrägstrich am Ende)
CORS_ORIGIN=http://localhost:4200

# Auth: JWT-Geheimnis (mind. 32 Zeichen, z. B. openssl rand -hex 32)
JWT_SECRET=dein-geheimes-jwt-secret-mind-32-zeichen

# Basis-URL der Frontend-App (für E-Mail-Links: Verifizierung, Passwort-Reset)
APP_URL=http://localhost:4200

# SMTP (optional): Ohne Angabe wird Ethereal-Test-Account verwendet (nur Dev)
# Production: Transactional-Provider nutzen (Resend, SendGrid, Mailgun), sonst oft 550 B-URL/Spam.
# From-Adresse muss zur SMTP-Domain passen (z. B. noreply@deinedomain.com).
# SMTP_HOST=smtp.resend.com
# SMTP_PORT=465
# SMTP_SECURE=true
# SMTP_USER=resend
# SMTP_PASSWORD=re_xxxx
# SMTP_FROM_EMAIL=noreply@deinedomain.com
# SMTP_FROM_NAME=Timeline

USE_PROD_DB=false

# Prod: Connection String (Vorrang) oder Einzelvariablen
# Session Pooler (Supabase, IPv4): User postgres.PROJECT_REF, Port 5432 – Host/Region aus Dashboard
# PROD_DB_URL=postgresql://postgres.PROJECT_REF:pass@aws-1-eu-west-2.pooler.supabase.com:5432/postgres
PROD_DB_HOST=
PROD_DB_PORT=5432
PROD_DB_NAME=
PROD_DB_USER=
PROD_DB_PASSWORD=
PROD_DB_SSL=false
PROD_DB_POOL_MODE=session
PROD_DB_ACQUIRE_TIMEOUT=30000

# Production: Verzeichnis für Uploads/Bilder (z. B. /var/data)
# Wenn gesetzt, werden Bilder hier gespeichert statt im Projektordner
# STORAGE_DIR=/var/data

